pipeline {
    agent any
    
    stages {
        stage ('Build Image'){
            steps {
                script {
                    dockerapp = docker.build("app/nodeapp", '-f ./src/Dockerfile ./src')
                }
            }
        }
    }
}