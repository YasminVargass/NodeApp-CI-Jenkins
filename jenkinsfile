pipeline {
    agent any
    
    stages {
        stage ('Build Image'){
            steps {
                script {
                    dockerapp = docker.build("app/nodeapp:${env.BUILD_ID}", '-f ./Dockerfile .')
                }
            }
        }
    }
}